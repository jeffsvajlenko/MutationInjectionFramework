	{
        X X = X ().X (X.X (), X.X ());
        if (X != X) {
            X = X.X ();
            if (X instanceof X) {
                X ((X) X);
                X.X (X, X, X);
            } else {
                X ();
            }
        } else {
            X ();
        }
    }
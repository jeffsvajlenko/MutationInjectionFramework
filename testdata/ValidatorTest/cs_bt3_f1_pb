        {
            X [] X = this.X ();
            for (X X = 0; X < X.X; X ++) {
                X X = X [X];
                if (X.X (X)) {
                    this.X (X);
                    this.X (X);
                }
            }
        }
